class CompletedChallenges {
  final String title;
  final int points;
  final DateTime completedAt;

  CompletedChallenges({
    required this.title,
    required this.points,
    required this.completedAt,
  });
}